#include<bits/stdc++.h>
using namespace std;
int main(){
    // Initialization
    char ch[100];

    // Input
    // cin>>ch; // omits the text after space,tab and enter
    cin.getline(ch,100);

    //Output
    cout<<"Output of ch is: "<< ch<<endl;

    // value of null char
    // char temp = ch[5];
    // int value = (int)temp;
    // cout<< "val of null char: "<< value<<endl;
}

